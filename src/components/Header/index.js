import React from "react";
import "./style.css";

function Header() {
  return (
    <header className="header">
      <h1>DBZ Memory Click Game!</h1>
      <h2>Click on each image to earn points, but don't click on any more than once per round or the game will reset!</h2>
    </header>
  );
}

export default Header;
